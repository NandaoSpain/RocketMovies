import styled from "styled-components";

export const Container = styled.button`
    width: 20px;
    height: 20px;
    background: transparent;
    border: none;

    > svg {
        color: #ccc;
    }

` 